<!DOCTYPE page PUBLIC
          "-//JBoss/Seam Pages Configuration DTD 1.2//EN"
          "http://jboss.com/products/seam/pages-1.2.dtd">

<page>
   <param name="firstResult" value="#{userList.firstResult}"/>
   <param name="order" value="#{userList.order}"/>
   <param name="from"/>
   <param name="email" value="#{userList.user.email}"/>
   <param name="fullName" value="#{userList.user.fullName}"/>
   <param name="password" value="#{userList.user.password}"/>
   <param name="userName" value="#{userList.user.userName}"/>
   <param name="role" value="#{userList.user.role}"/>
</page>